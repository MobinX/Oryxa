{
  "installCommand": "cd ../.. && bun install",
  "buildCommand": "echo 'API: no compile step — Vercel bundles api/index.ts'",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
